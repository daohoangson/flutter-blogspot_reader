{
  "name": "firestore",
  "scripts": {
    "deploy": "npm test && firebase deploy --only firestore:rules",
    "emulator": "firebase emulators:start --only firestore",
    "format": "prettier --write **/*.js",
    "test": "mocha --timeout=10000"
  },
  "private": true,
  "devDependencies": {
    "@firebase/rules-unit-testing": "^1.3.5",
    "filesystem": "^1.0.1",
    "mocha": "^9.0.0",
    "prettier": "^1.18.2"
  }
}
